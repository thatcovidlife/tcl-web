# Active Context

## Current Task

Added Sentry spans around database calls in all files in the `server/api` directory and added Sentry user identification to the `updateUserInfo` function in `store/user.ts`.

## Recent Changes

1. Added Sentry spans around database calls in all files in the `server/api` directory
2. Added Sentry user identification to the `updateUserInfo` function in `store/user.ts`

## Next Steps

- Complete documentation of Sentry changes in memory bank files
